# Optional: where the frontend should send API requests.
#
# Local dev:
# - Leave blank to use Vite proxy (/api -> http://localhost:5000)
# - OR set explicitly:
#   VITE_API_BASE_URL=http://localhost:5000
#
# Render (if frontend and backend are separate services):
#   VITE_API_BASE_URL=https://ecom-group.onrender.com
VITE_API_BASE_URL=
